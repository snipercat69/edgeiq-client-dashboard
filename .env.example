# Auth token for dashboard login
# If blank, a random token is auto-generated and printed to console on first run
AUTH_TOKEN=

# HTTP port for the dashboard server (default: 8080)
DASHBOARD_PORT=8080

# Directory where client JSON files are stored
# Default: <app_dir>/clients
DASHBOARD_DATA_DIR=
